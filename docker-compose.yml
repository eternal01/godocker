#--------------------------------------------------------------------------
# Core Compose - Workspace Only
#--------------------------------------------------------------------------
# 此文件仅包含 workspace 服务、网络和核心缓存卷定义。
# 所有基础设施服务已拆分到 compose/*.yml 中，按需组合启动。
#
# 用法示例:
#   docker compose up -d                          # 仅启动 workspace
#   docker compose -f docker-compose.yml -f compose/db.yml up -d
#--------------------------------------------------------------------------

networks:
  frontend:
    driver: ${NETWORKS_DRIVER}
  backend:
    driver: ${NETWORKS_DRIVER}

volumes:
  workspace-mise-cache:
    driver: ${VOLUMES_DRIVER}
  workspace-brew-cache:
    driver: ${VOLUMES_DRIVER}
  workspace-cargo-registry:
    driver: ${VOLUMES_DRIVER}
  workspace-cargo-git:
    driver: ${VOLUMES_DRIVER}
  workspace-go-mod:
    driver: ${VOLUMES_DRIVER}
  workspace-composer:
    driver: ${VOLUMES_DRIVER}
  mysql:
    driver: ${VOLUMES_DRIVER}
  redis:
    driver: ${VOLUMES_DRIVER}
  mongo:
    driver: ${VOLUMES_DRIVER}
  minio:
    driver: ${VOLUMES_DRIVER}
  elasticsearch:
    driver: ${VOLUMES_DRIVER}
  postgres:
    driver: ${VOLUMES_DRIVER}

services:

  ### Workspace #############################################
  workspace:
    build:
      context: .
      dockerfile: workspaces/workspace.Dockerfile
      # Use the normal build network by default. Set WORKSPACE_BUILD_NETWORK=host
      # only when the proxy listens on 127.0.0.1 on the Docker host.
      network: ${WORKSPACE_BUILD_NETWORK}
      extra_hosts:
        - "host.docker.internal:host-gateway"
      args:
        - SYSTEM_NAME=${SYSTEM_NAME}
        - SYSTEM_VERSION=${SYSTEM_VERSION}
        - WORKSPACE_USER=${WORKSPACE_USER}
        - WORKSPACE_HOME=${WORKSPACE_HOME}
        - WORKSPACE_PATH=${APP_CODE_PATH_CONTAINER}
        - WORKSPACE_INSTALL_BREW=${WORKSPACE_INSTALL_BREW}
        - WORKSPACE_PREINSTALL_LANGUAGES=${WORKSPACE_PREINSTALL_LANGUAGES}
        - WORKSPACE_INSTALL_DNSUTILS=${WORKSPACE_INSTALL_DNSUTILS}
        - WORKSPACE_INSTALL_WORKSPACE_SSH=${WORKSPACE_INSTALL_WORKSPACE_SSH}
        - WORKSPACE_BREW_PACKAGES=${WORKSPACE_BREW_PACKAGES}
        - MISE_VERSION=${MISE_VERSION}
        - PUID=${WORKSPACE_PUID}
        - PGID=${WORKSPACE_PGID}
        - TZ=${TZ}
        - HTTP_PROXY=${HTTP_PROXY}
        - HTTPS_PROXY=${HTTPS_PROXY}
        - NO_PROXY=${NO_PROXY}
    volumes:
      - ${APP_CODE_PATH_HOST}:${APP_CODE_PATH_CONTAINER}${APP_CODE_CONTAINER_FLAG}
      # Keep cache persistent, but keep mise installs in the image layer so
      # build-time preinstallation is not hidden by an empty volume.
      - workspace-mise-cache:${WORKSPACE_HOME}/.cache/mise
      # language-specific caches
      - workspace-go-mod:${WORKSPACE_HOME}/go/pkg/mod
      - workspace-cargo-registry:${WORKSPACE_HOME}/.cargo/registry
      - workspace-cargo-git:${WORKSPACE_HOME}/.cargo/git
      - workspace-composer:${WORKSPACE_HOME}/.composer
      # Keep Homebrew downloads persistent, but do not overlay its prefix.
      - workspace-brew-cache:/home/linuxbrew/.cache/Homebrew
    extra_hosts:
      # Allow workspace to reach a proxy / host services on the host. Linux
      # Docker (no host.docker.internal by default) still resolves via the
      # host-gateway magic IP since Compose v2.
      - "host.docker.internal:host-gateway"
    environment:
      # Propagate proxy to go install / pip / brew / curl / etc. inside the
      # container. Default in .env is empty, which disables the proxy.
      - HTTP_PROXY=${HTTP_PROXY}
      - HTTPS_PROXY=${HTTPS_PROXY}
      - NO_PROXY=${NO_PROXY}
      - http_proxy=${HTTP_PROXY}
      - https_proxy=${HTTPS_PROXY}
      - no_proxy=${NO_PROXY}
      - WORKSPACE_AUTO_DETECT=${WORKSPACE_AUTO_DETECT}
    ports:
      - "${WORKSPACE_SSH_PORT}:22"
      - "${WORKSPACE_SERVE_HOST_PORT}:1080"
    tty: true
    networks:
      - frontend
      - backend
    restart: always
